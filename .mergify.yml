pull_request_rules:
  - name: BB merge date boundary
    conditions:
      - title = BB merge date gate canary
      - check-success = BB_DATE_GATE_20260810_F631
    actions:
      merge:
        method: merge
